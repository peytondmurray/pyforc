import numpy as np


def find_first_not_nan(arr):

    # i = np.argwhere(np.logical_not(np.isnan(arr)))[0]

    # np.where

    # for i in range(arr.shape[0]):
    #     if not np.isnan(arr[i]):
    #         return arr[i]

    # return ValueError("")